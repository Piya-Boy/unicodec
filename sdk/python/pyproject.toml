[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ubc"
version = "0.1.0"
description = "Universal Binary Container reference SDK"
requires-python = ">=3.11"
dependencies = ["cryptography>=42"]

[tool.setuptools]
packages = ["ubc"]
